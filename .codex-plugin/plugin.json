{
  "name": "codex-ralph-plugin",
  "version": "0.1.0",
  "description": "Adds a minimal opt-in Stop hook for $codex-ralph-plugin.",
  "skills": "./skills/",
  "hooks": "./hooks/hooks.json",
  "interface": {
    "displayName": "Codex Ralph Plugin",
    "shortDescription": "Continue a turn on demand",
    "longDescription": "Runs a minimal Stop hook only when the user explicitly writes $codex-ralph-plugin. The first number is the retry limit, not task content.",
    "category": "Productivity",
    "capabilities": [
      "Stop hook",
      "Guarded continuation"
    ],
    "defaultPrompt": "$codex-ralph-plugin 5 acceptance: tests pass."
  }
}
